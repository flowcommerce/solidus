$(document).ready(function() {
  var client = contentful.createClient({
    space: 'jfcmd71weej6',
    accessToken: '13f05238e42072e0bcb7c17e14fce5ecbd22c746adef3e7e5a5dfd193f58e8a7'
  });

  client.getEntries({
    'content_type': 'categoryHeroImage'
  })
  .then(function (entries) {
    var url = entries.items[0].fields.firstHeroImage.fields.file.url;
    $("<img src='" + url + "' />").appendTo(".banner-image");
  });
});
