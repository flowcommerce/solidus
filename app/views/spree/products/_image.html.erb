<%=
  img_src = if defined?(image) && image
      image.attachment.url(:large)
    else
      @product.display_image.attachment(:large)
    end

  image_tag img_src, itemprop: "image", id: 'main-image'
%>
